interface:
  display_name: "Debug"
  short_description: "Investigate the root cause before fixing a failure."
  default_prompt: "$debug <symptom or failing test>"
policy:
  allow_implicit_invocation: true
