interface:
  display_name: "Think"
  short_description: "Turn an idea or design question into an explicit decision."
  default_prompt: "$think <idea or design question>"
policy:
  allow_implicit_invocation: true
