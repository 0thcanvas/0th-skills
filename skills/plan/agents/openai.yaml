interface:
  display_name: "Plan"
  short_description: "Plan vertical slices and lock API, cost, authorization, and live-call boundaries."
  default_prompt: "$plan <decision record path or scope>"
policy:
  allow_implicit_invocation: true
