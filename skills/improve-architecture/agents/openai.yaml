interface:
  display_name: "Improve Architecture"
  short_description: "Find deepening opportunities — turn shallow modules into deep ones."
  default_prompt: "$improve-architecture <area to focus on, optional>"
policy:
  allow_implicit_invocation: true
