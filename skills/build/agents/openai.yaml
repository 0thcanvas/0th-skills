interface:
  display_name: "Build"
  short_description: "Implement changes with TDD on a feature branch."
  default_prompt: "$build <instruction or plan path>"
policy:
  allow_implicit_invocation: true
