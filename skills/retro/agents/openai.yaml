interface:
  display_name: "Retro"
  short_description: "Capture user corrections and agent misfires into the persistent incident log; surface patterns at threshold."
  default_prompt: "$retro <optional: hint at which corrections to capture>"
policy:
  allow_implicit_invocation: true
