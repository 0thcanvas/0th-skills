Analyze only the supplied source bucket. Return every literal source, claim, caveat, and
contradiction identifier present in that bucket. Preserve a contradiction identifier even when the
bucket alone cannot resolve it. Do not use tools, inspect the workspace, or infer identifiers from
outside knowledge.
