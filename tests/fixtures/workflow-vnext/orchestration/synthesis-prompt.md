Synthesize only the three worker outputs. Return the union of every source identifier and every
contradiction identifier. A contradiction remains unresolved unless the worker outputs contain an
explicit resolution record; do not discard it merely because one claim seems newer. Give a concise
conclusion, but do not use tools or inspect the workspace.
