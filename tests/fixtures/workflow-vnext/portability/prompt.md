Read only the canonical packet below. Echo its declared SHA-256 value as `packet_hash`, return every
record id in packet order, return every open-loop id in packet order, and return the value of the
decision record. Do not use tools, skills, memory, files, or outside knowledge.
