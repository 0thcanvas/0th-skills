# CONDITION POLICY

Before editing source, create `.eval/plan.md` with the goal, ordered implementation steps,
dependencies, acceptance criteria, and intended verification. Follow it and mark the steps complete
or revised as work proceeds. Do not create `.eval/state.json`.
