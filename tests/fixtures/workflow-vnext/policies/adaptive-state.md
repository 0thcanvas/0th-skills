# CONDITION POLICY

Inspect enough evidence to choose the next action, then create `.eval/state.json` before editing
source. Keep only `goal`, `evidence`, `current_hypothesis`, `next_action`, and `verification_status`.
Update it when evidence changes the hypothesis. Do not write an up-front step-by-step plan and do not
create `.eval/plan.md`.
