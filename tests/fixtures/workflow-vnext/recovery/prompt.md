You are resuming a research task in a fresh context.

Use only the handoff artifact below. Return every explicit identifier that the artifact preserves,
grouped into the six requested arrays. Do not invent identifiers, reconstruct identifiers from
prose, or add identifiers that are not literally present in the artifact. Empty arrays are valid.
Do not use tools or inspect the workspace.
