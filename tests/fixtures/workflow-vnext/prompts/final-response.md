# FINAL RESPONSE

Return the required JSON object only. `summary` states what changed, `root_cause` states the diagnosed
cause when applicable (or `not-applicable`), and `verification` lists the exact local commands run.
